import { InjectionToken } from '@angular/core';

/** Ruta relativa del recurso de listado bajo `API_BASE_URL`; ajustar tras inventario legado (catálogo fila 3). */
export const COTIZACIONES_LIST_RELATIVE_PATH = new InjectionToken<string>(
  'COTIZACIONES_LIST_RELATIVE_PATH'
);
